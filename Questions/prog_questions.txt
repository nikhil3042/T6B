1. strs = ["bat",'ate','eat']
op = [['bat'], ['ate', 'eat']]

group anagrams together

2. l=[1,1,1,1,3]
op=[1, 3, '-', '-', '-']

the repeated values are replaced by '-' in the last

3.
ex.1 st=['flower','flow','flight']   
     output = fl
ex2 st= ['car','cir']
	output = c
ex3 st = ['abc','sef']
	output = ''

4. decimal to binary

5. decimal to octal

6. decimal to hexadecimal

7.you have 5 bikes capacity of 10l and milage of 10kmpl. what will the maximum distance you can go. 
you can shift fuel
you can get rid of bikes 
you cant go back
Make the program generalised 

8. inp = 'I WOrk In SoNY'
op = 'I KRow Ni YnOS'

9. Write a Python program that takes an integer input from the user and inserts it into the list using the following logic:
1 = [20, 30, 10, 8, 50]
 1. If the number already exists in the list, insert it right after its first occurrence.
 2. If the number does not exist:
 • Find the smallest number in the list that is greater than the input, and insert the input just before that number.
 • If no greater number exists, insert the input at the end of the list.

10. selection sort
